[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-bootstrap"
version = "0.1.0"
description = "Agent-First Project Bootstrap CLI"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
agent-bootstrap = "agent_bootstrap:main"

[project.optional-dependencies]
dev = ["pytest"]